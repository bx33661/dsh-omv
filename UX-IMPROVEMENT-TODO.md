# OMV 前端体验整改清单

这份清单记录插件前端的八项体验整改，目标是保持 DSH 原生视觉、交互和组件习惯，让 OMV 更像一个可操作的审计工作台，而不是一组 AI 仪表盘。

## 已完成

- [x] 详情页操作层级：保留单一主动作，其余动作收进“更多操作”，永久删除保留二次确认。
- [x] 搜索体验：去掉重复搜索图标；结果按漏洞发现、审计任务、活动记录分组；可定位的活动记录可直接打开目标；补充键盘打开和结果元信息。
- [x] 详情页信息分区：增加概览、证据、复现、历史四个工作视图，默认进入概览。
- [x] 操作反馈：详情主操作、校验、归档、删除、恢复显示进行中状态并锁定重复提交。
- [x] 漏洞列表：增加带数量的工作范围筛选、详细状态筛选和按优先级、最近变化、证据成熟度、阻塞、可提交排序。
- [x] 术语统一：运行产物替代“附件 / artifact”作为用户可见主称呼，协议字段保留英文标识。
- [x] 异常处理：Evidence / Campaign 配置异常支持打开文件、复制路径、单项重载或刷新工作区。
- [x] DSH 原生复用：继续使用 DSH Input、Button、Modal、CodeBlock、TerminalBlock、Toast 等原生控件，新增控件沿用现有 token 和焦点行为。

## 验收标准

- 默认视图先回答“现在有什么、下一步是什么”，不要求用户先读图表。
- 所有会改变 Evidence、Campaign 或 Run 状态的操作都有明确的进行中、成功或失败反馈。
- 列表筛选、搜索结果、详情操作均支持键盘使用。
- 不新增独立配色体系；颜色只表达状态、优先级和可操作性。
- 数据部分损坏时保留其他工作区内容，并提供可恢复路径。
